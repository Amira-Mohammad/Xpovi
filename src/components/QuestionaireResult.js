import React from 'react';

const QuestionaireResult = () => {
    return (
        <div>
            QuestionaireResult
            <button type="button" className="btn btn-primary submitBtn position-absolute">Submit</button>
        </div>
    );
};

export default QuestionaireResult;