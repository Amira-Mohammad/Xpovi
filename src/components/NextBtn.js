import React from 'react';

const NextBtn = () => {
    return (
        <div>
            <button type="button" className="btn btn-primary next2Section2 position-absolute">Next</button>
        </div>
    );
};

export default NextBtn;